<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".MainActivity">

    <ImageView
        android:id="@+id/account_avatar"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_centerInParent="true"
        android:layout_margin="20dp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:ignore="ContentDescription" />

    <TextView
        android:id="@+id/account_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/text_placeholder"
        android:layout_marginHorizontal="20dp"
        app:layout_constraintLeft_toRightOf="@id/account_avatar"
        app:layout_constraintTop_toTopOf="@id/account_avatar" />

    <TextView
        android:id="@+id/account_server"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/text_placeholder"
        android:layout_marginHorizontal="20dp"
        app:layout_constraintLeft_toRightOf="@id/account_avatar"
        app:layout_constraintBottom_toBottomOf="@id/account_avatar"/>

    <ImageView
        android:id="@+id/account_search_button"
        android:src="@drawable/ic_baseline_search_40"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_margin="20dp"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:ignore="ContentDescription" />

    <TextView
        android:id="@+id/account_recently_sync"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/text_placeholder"
        android:layout_marginHorizontal="20dp"
        android:layout_marginVertical="10dp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toBottomOf="@id/account_avatar"/>

    <ImageView
        android:id="@+id/account_sync_now"
        android:src="@drawable/ic_baseline_refresh_24"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_marginHorizontal="20dp"
        app:layout_constraintLeft_toRightOf="@id/account_recently_sync"
        app:layout_constraintTop_toTopOf="@id/account_recently_sync"
        tools:ignore="ContentDescription" />

</androidx.constraintlayout.widget.ConstraintLayout>